{
  "name": "roket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "bash init-install.sh || init-install.bat",
    "start": "npm-run-all -p start-backend start-frontend",
    "start-backend": "cd api && npm start",
    "start-frontend": "cd app && ng serve --port=3000 --open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/leaflet": "^1.9.8",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "ol": "^8.2.0"
  }
}
